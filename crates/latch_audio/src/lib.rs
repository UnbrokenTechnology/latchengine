//! Latch Audio System
//!
//! Cross-platform audio playback and mixing

/// Audio system (placeholder for Phase 1+)
pub struct AudioSystem {
    _placeholder: (),
}

impl AudioSystem {
    pub fn new() -> Self {
        Self { _placeholder: () }
    }
}

impl Default for AudioSystem {
    fn default() -> Self {
        Self::new()
    }
}
